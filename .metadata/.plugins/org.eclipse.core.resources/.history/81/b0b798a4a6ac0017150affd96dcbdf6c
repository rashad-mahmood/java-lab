package com.inprogress;

import java.util.AbstractCollection;
import java.util.List;

public class NeuralNetwork {
	
	Neuron[] input;
	Neuron[] hidden;
	Neuron[] output;
	
	public NeuralNetwork(int inputLayerSize, int hiddenLayerSize, int outputLayerSize) {
		super();
		this.input = new Neuron[inputLayerSize];
		this.hidden = new Neuron[hiddenLayerSize];
		this.output = new Neuron[outputLayerSize];
	}
	
	public void createNetwork() {
		for (int i  = 0; i < inputLayerSize; i++) {
			Neuron in = new Neuron();
			for (int h = 0; i < hiddenLayerSize; h++) {
				Neuron hn = new Neuron();
				
			}
		}
	}
	

}
